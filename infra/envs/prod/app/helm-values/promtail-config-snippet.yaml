# promtail-config-snippet.yaml
pipeline_stages:
  - docker: {}
  - match:
      selector: '{app="t6-on-race-scg"}'
      stages:
        - labels:
            job: scg-gateway # Loki에서 검색할 때 사용할 레이블
            env: prod